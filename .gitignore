# --- Python ---
__pycache__/
*.py[cod]
.venv/
.uv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# --- Node / Vite ---
web/node_modules/
web/dist/
web/.vite/

# --- Raw INDEC microdata (large, re-downloadable, pinned by checksum) — never commit ---
data/raw/

# --- Secrets / local env ---
.env
.env.*
!.env.example

# --- Editor / IDE / tooling (machine-specific) ---
.claude/
.vscode/
.idea/
*.log

# --- OS ---
.DS_Store
Thumbs.db

# --- Local planning notes (kept out of the public repo) ---
PLAN.md

# --- Transient output of pipeline.watch when run locally ---
eph-watch-body.md
eph-bump-pr-body.md

# NOTE: data/percentiles.v1.json and web/public/percentiles.v1.json ARE committed on
# purpose — the precomputed, provenance-stamped artifact is the deliverable.
